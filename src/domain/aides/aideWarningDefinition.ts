export type AideWarningDefinition = {
  aide_cms_id: string;
  last_month?: boolean;
  last_month_sent?: boolean;
  last_week?: boolean;
  last_week_sent?: boolean;
  expired?: boolean;
  expired_sent?: boolean;
};
