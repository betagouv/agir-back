import { Tag_v2 } from './Tag_v2';

export const EditorialTagList: { [key in Tag_v2]?: string } = {
  contenu_important: `Tag système indiquant qu'un contenu est de facto recommandé, équivalent d'un tag qui matche le profile utilisateur`,
};
